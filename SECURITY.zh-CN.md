# 安全策略

> [English](SECURITY.md)

## 支持范围

| 版本 | 支持情况 |
|---|---|
| `master` | 预发布开发期间接收安全修复 |
| 已发布标签 | 尚未承诺生产支持 |

## 报告漏洞

请勿在公开 Issue 中报告漏洞。使用仓库的 [GitHub 私密漏洞报告](https://github.com/tyj1987/broker/security/advisories/new)，说明受影响版本、影响、前提条件和最小复现。不要提交真实凭据，也不要在生产环境测试。

除非仓库已验证的安全页面另行公告，本项目不承诺公开奖金或响应时限。

## 安全边界

- 严格档自动化只允许版本化、类型化操作，不接受调用方提供的任意 URL、认证 Header 或命令。
- 公网反向代理验证外部客户端证书，并以独立的代理工作负载证书连接 Broker。
- WebAuthn、短期会话、工作负载身份、策略、速率限制和撤销是相互独立的控制层。
- SOPS 保护静态凭据；日志、错误和业务响应必须经过统一脱敏。
- AI 与不受信任自动化不得获得长期明文凭据。

完整设计和已知剩余风险见 [`docs/THREAT-MODEL.md`](docs/THREAT-MODEL.md) 与发布验收记录。自动测试通过不等于生产安全验收通过。

## 部署检查

- 非 root、只读根文件系统、删除全部 capability，并启用 `no-new-privileges`。
- 后端仅监听本机或私网；nginx 到 Broker 强制验证 CA、主机名和代理工作负载证书。
- 生产凭据不进入仓库、镜像、日志、CI artifact 或对话。
- 每次发布验证 commit、制品 SHA-256、镜像 digest、SBOM、签名和来源证明。
- 凭据撤销、KMS 故障、证书轮换、回滚及灾备恢复必须有演练证据。

## 密码学与审计

传输最低使用 TLS 1.2，优先 TLS 1.3。审计事件使用结构化字段、请求 ID、统一脱敏和防篡改存储；本地开发默认写入 `audit/YYYY-MM-DD.jsonl`，生产必须另行配置独立审计存储。
