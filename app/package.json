{
  "name": "my-first-app",
  "version": "0.1.0",
  "private": true,
  "description": "First project using the dev system blueprint",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --env-file=../.env --watch index.js",
    "start": "node --env-file=../.env index.js",
    "lint": "echo \"(no linter configured yet)\"",
    "format": "echo \"(no formatter configured yet)\"",
    "test": "node --test test/",
    "test:coverage": "echo \"(coverage tool not configured yet)\""
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "pg": "^8.11.5",
    "redis": "^4.6.13"
  }
}
