# Install as /etc/sudoers.d/secret-broker-deploy, owned by root:root, mode 0440.
# The helper validates that it receives exactly one 40-character commit SHA.
broker-deploy ALL=(root) NOPASSWD: /usr/local/sbin/secret-broker-deploy *
