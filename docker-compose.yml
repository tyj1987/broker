# docker-compose.yml for Secret Broker V4.1
#
# 主服务: Secret Broker (mTLS HTTPS, port 8443)
# 可选: 演示 app (profiles: example)
#
# 用法:
#   1. 准备 PKI + 加密 secrets
#      - ./pki/ca/ca.crt
#      - ./pki/server/server.crt + server.key
#      - ./pki/clients/*.crt+key
#      - ./secrets/broker.yaml       (SOPS 加密)
#      - ./secrets/secrets-detail.json (SOPS 加密)
#   2. docker compose up -d broker
#   3. 验证: docker compose exec broker wget -qO- https://localhost:8443/health

name: secret-broker

services:

  # ==========================================================
  # 主服务: Secret Broker
  # ==========================================================
  broker:
    image: ghcr.io/tyj1987/broker@${BROKER_IMAGE_DIGEST:?set BROKER_IMAGE_DIGEST to the verified sha256 digest}
    container_name: secret-broker
    restart: unless-stopped
    ports:
      - "127.0.0.1:8443:8443" # backend is reachable only by the local trusted proxy
    volumes:
      - ./secrets:/var/lib/broker/secrets:ro
      - ./pki:/run/secrets/broker/pki:ro
      - broker-audit:/var/lib/broker/audit
      - broker-tmp:/tmp:size=64M
    environment:
      - NODE_ENV=production
      - BROKER_BIND=0.0.0.0
      - BROKER_PORT=8443
      - BROKER_PKI_DIR=/run/secrets/broker/pki
      - BROKER_AUDIT_DIR=/var/lib/broker/audit
      - BROKER_SECRETS_DETAIL_PATH=/var/lib/broker/secrets/secrets-detail.json
      - BROKER_CONFIG_PATH=/var/lib/broker/secrets/broker.yaml
      - BROKER_HEALTH_SOCKET_PATH=/tmp/broker-health.sock
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      # distroless has no shell/curl — use node's https module directly
      test:
        - "/nodejs/bin/node"
        - "-e"
        - "require('node:http').get({socketPath:'/tmp/broker-health.sock',path:'/health'},r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - broker-net
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.2'
          memory: 256M
    restart: unless-stopped

  # ==========================================================
  # 可选: Prometheus (监控)
  # ==========================================================
  prometheus:
    image: prom/prometheus:v2.54.1@sha256:f6639335d34a77d9d9db382b92eeb7fc00934be8eae81dbc03b31cfe90411a94
    container_name: broker-prometheus
    profiles: ["monitoring"]
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./deploy/grafana/alerts.yml:/etc/prometheus/alerts.yml:ro
      - prometheus-data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/usr/share/prometheus/console_libraries"
      - "--web.console.templates=/usr/share/prometheus/consoles"
    networks:
      - broker-net

  # ==========================================================
  # 可选: Grafana (可视化)
  # ==========================================================
  grafana:
    image: grafana/grafana:11.2.0@sha256:408afb9726de5122b00a2576763a8a57a3c86d5b0eff5305bc994ceb3eb96c3f
    container_name: broker-grafana
    profiles: ["monitoring"]
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./deploy/grafana/dashboard.json:/var/lib/grafana/dashboards/broker.json:ro
      - ./deploy/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./deploy/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - grafana-data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:?set GRAFANA_ADMIN_PASSWORD}
    networks:
      - broker-net

volumes:
  broker-audit:
  broker-tmp:
  prometheus-data:
  grafana-data:

networks:
  broker-net:
    driver: bridge
